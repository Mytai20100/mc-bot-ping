ê làm 1 dự án mc-bot-ping với repo github sẽ xài https://github.com/Mytai20100/mc-bot-ping làm con bot discord bằng nodejs với thư viện mới nhất ổn định nhanh với các tính năng như:





Có lệnh /setup create <tên kênh> hoặc /setup #tên kênh để bot join vô kênh đó đọc tin nhắn ( chỉ có admin mới xài đc )



có lệnh !ping để test độ delay từ bot đến server discord



!pingserver <ip, domain : port>và /pingserver <ip, domain : port> để nó đo ping đến server minecraft đó



!infoserver <ip, domain : port> và /infoserver <ip, domain : port>để đọc các thông tin cơ bản của server lấy icon của server đó tên , sống lượng người chơi MOTD , version , bootstap đầy đủ như trên client minecraft hiện rồi nó tạo 1 hình canvas gửi rep lại người gửi ip đó



Nhắn trực tiếp trong kênh mà bot đc setup đọc tin nhắn qua /setup hoặc /setup create cũng tạo 1 hình canvas  gửi rep lại người gửi ip đó như cái !infoserver



!info hiện các thông tin cơ bản bằng hình canvas  như version , tên , uptime , icon của bot , lv như 90000 , xp -1e+23 , rồi bên dưới kèm theo 1 link fornt chữ như viết tay với link https://github.com/Mytai20100/mc-bot-ping



có Presence Status idle và có Activity là playing số server đã được join 



tất cả đều là tiếng anh  có thên /lang gồm en với vi bot sẽ gi nhớ người dùng nào /lang <ngôn ngữ> để trả lời và tạo canva hợp lí 

